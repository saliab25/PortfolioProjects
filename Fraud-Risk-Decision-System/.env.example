# Development-only values. Copy this file to .env and choose a local password.
POSTGRES_DB=fraud_risk
POSTGRES_USER=fraud_app
POSTGRES_PASSWORD=change-me-for-local-development
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

